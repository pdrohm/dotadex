import React from "react";

type Props = {
  search: string;
};

const FilterBar = (props: Props) => {
  return (
    <div className="h-10 w-full mx-auto bg-background-dark/10 flex items-center justify-center">
      TESTE
    </div>
  );
};

export default FilterBar;
